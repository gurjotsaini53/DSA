#include <iostream>
using namespace std;
#define max 10;
int main()
{
    int n;
    cout << "Enter size of Array = ";
    cin >> n;
    int arr[n];
    for (int i = 0; i < n; i++)
    {
        arr[i] = rand() % 10;
        g
        // scale random number in the range 0 to max-1;
    }
    for (int i = 0; i < n; i++)
    {
        cout << "arr[" << i << "] = " << arr[i] << endl;
    }
}